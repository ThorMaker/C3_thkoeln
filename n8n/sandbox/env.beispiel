# Als .env neben docker-compose.yml speichern (cp env.beispiel .env) und Schlüssel erzeugen:
#   openssl rand -hex 32
N8N_ENCRYPTION_KEY=bitte-ersetzen
# Version fest vorgeben, damit die Sandbox der Produktivinstanz entspricht, z.B. 2.38.7
N8N_VERSION=latest
